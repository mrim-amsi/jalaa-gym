workflows:
  android-build:
    name: Android Build
    environment:
      java: 17
    scripts:
      - ./gradlew clean
      - ./gradlew assembleDebug
    artifacts:
      - app/build/outputs/apk/debug/app-debug.apk
